const baseURL = ('https://jsonplaceholder.typicode.com');

const urls = {
    albums: '/albums',
    users: '/users',
    comments: '/comments',
    post: '/posts/id',
    todos: '/todos'
};

export {
    baseURL,
    urls
};