const baseURL = ('https://jsonplaceholder.com');

const urls = {
    albums: '/albums',
    users: '/users',
    comments: '/comments',
    post: '/posts/id',
    togos: '/togos'
};

export {
    baseURL,
    urls
};