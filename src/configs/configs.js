const baseURL = ('https://jsonplaceholder.typicode.com');

const urls = {
    albums: '/albums',
    users: '/users',
    comments: '/comments',
    post: '/posts',
    todos: '/todos'
};

export {
    baseURL,
    urls
};